export { default as CIcon } from './c-icon.vue';
// Type consumed by c-icon's `icon` prop. Defaults to `string` and can be
// augmented by consumers to their own union type derived from defineIcons().
export type CIconKey = string;

